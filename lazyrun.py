# for now we will just have a few scripts in the apps directory
# in the future these should be dynamically generated
# then we parse yaml config, aggregate it with other configs potentially
# and execute podman as needed 
